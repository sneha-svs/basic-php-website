apiVersion: v1
kind: Service
metadata:
  labels:
    app: php_practice1
  name: php_practice1
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: php_practice1
